<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Boutons-->
    <style name="bouton" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textColor">#000000</item>
        <item name="android:shadowColor">#FF000000</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">-1</item>
        <item name="android:shadowRadius">0.2</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

        <style name="boutonSuivant" parent="@style/bouton">
            <item name="android:background">@drawable/bouton_suivant</item>
            <item name="android:textColor">#FFFFFF</item>
        </style>

        <style name="boutonPrecedent" parent="@style/bouton">
            <item name="android:background">@drawable/bouton_precedent</item>
            <item name="android:textColor">#FFFFFF</item>
        </style>

</resources>